/**
 * Represents the controller interface, which holds
 * the run method that controls the entire photo editor.
 */
public interface IController {

  /**
* Represents the run method that controls the
* entire photo editor.
*/
  void run();
}
